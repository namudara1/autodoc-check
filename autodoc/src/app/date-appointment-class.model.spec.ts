import { DateAppointmentClass } from './date-appointment-class.model';

describe('DateAppointmentClass', () => {
  it('should create an instance', () => {
    expect(new DateAppointmentClass()).toBeTruthy();
  });
});
